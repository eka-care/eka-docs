### Care Context

Specific segments of healthcare data provided by an HIP, such as diagnostic reports or prescriptions.

**How does the API’s works:**

1. **Get Linked Providers :** This API retrieves the list of providers (e.g., hospitals) linked to the user. And it returns a list with provider names and their corresponding IDs.
2. **Get Linked Care Contexts :** This API retrieves the list of care contexts linked to hip ID. And it returns a list of care contexts with details such as `care_context_id`, `created_at`,`status.`