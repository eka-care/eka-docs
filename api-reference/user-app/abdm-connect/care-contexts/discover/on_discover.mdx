---
openapi: post /abdm/v1/care-contexts/on-discover
---

<Note>
Call this API in response to the [Discovery Request Received webhook](/api-reference/user-app/abdm-connect/webhooks/discover-care-context). Use the `request_id` and `txn_id` from the webhook payload when constructing your response.
</Note>