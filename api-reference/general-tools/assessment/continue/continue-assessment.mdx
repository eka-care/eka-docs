---
openapi: put /assessment/api/v1/continue/{hash}/{qid}
---
<Note>
The `hash` in the URL is the unique identifier for the assessment which was provided in the response of init assessment API. The `qid` in the URL is the unique identifier for the question starting from 0 (first question).
</Note>

### Response Format
---
The request body must contain the answer to the previous question.
The format of response for different question component should be according to the following format for different question components.

<AccordionGroup>
  <Accordion title="Autosuggest (I-ATSG)">
```json
{
    "response": [
        {
            "choice_id": "s-2488272578",    //id of the option selected from previous response (❗️required)
            "choice_text": "Cough"  //  choice (text) selected from previous response (optional)"
        }
    ]
}
```
  </Accordion>

  <Accordion title="Single Select (I-RADO)">
```json
{
    "response": [
        {
            "choice_id": "s_4725efa4-a65c-11eb-8d02-1e003a340630", // id of the option selected from previous response (❗️required)
            "choice_text": "Over a period of time" // choice (text) selected from previous response (optional)
        }
    ] 
}
```
  </Accordion>
  <Accordion title="Multi-Select (I-MULT)">
```json
{
    "response": [
        {
            "choice_id": "s_b87e7437-1305-4973-b7f7-7b07f51fb80e",  // (❗️required)
            "choice_text": "Red skin rash"  // (optional)
        },
        {
            "choice_id": "s_4720607a-a65c-11eb-8d02-1e003a340630",  // (❗️required)
            "choice_text": "Headache"   // (optional)
        }
    ]
}
// 📌 NOTE THE MULTIPLE ENTRIES FOR `SELECTED_CHOICES` IN MULTIPLE CHOICES COMPONENT
```
  </Accordion>
  <Accordion title="Radio Group (I-RADG)">
```json
{
    "response": [
        {
        "choice_id": "s_4726a78c-a65c-11eb-8d02-1e003a340630",  //(❗️required)
        "choice_qualifier": "p",     // p/a/u for positive/absent/unknown (❗️required)
        "choice_text": "Medication"  // (optional)
        }
    ]
}
```
  </Accordion>
</AccordionGroup>

<Tip>
Refer to the [list component](/api-reference/general-tools/assessment/component-info/list-all-components) documentation for different question compoent response format. 
</Tip>
