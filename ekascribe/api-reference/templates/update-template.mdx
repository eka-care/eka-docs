---
title: "Update Template"
openapi: "PATCH /voice/api/v1/template/{template_id}"
---

Update a custom template's `title` or `desc` — only the fields you send are changed. Eka default templates cannot be modified.
